# Runtime dependencies for the hosted dashboard (Streamlit Community Cloud reads this file).
# Mirrors the runtime deps in pyproject.toml, plus `anthropic` so the bring-your-own-key AI
# features (Ask tab + roadmap) work when a visitor supplies their own key. Dev/orchestration
# extras are intentionally excluded.
httpx>=0.27
tenacity>=8.2
pydantic>=2.6
pandas>=2.2
duckdb>=1.0
pyarrow>=15
pytz>=2024.1
streamlit>=1.33
altair>=5
anthropic>=0.40
